<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Network ── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ── Location (for distance calculation in swipe deck + city auto-detect) ── -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ── Camera (for face verification + profile photos) ── -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ── Microphone (for in-chat voice messages via MediaRecorder) ── -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ── Notifications (Android 13+ requires runtime permission) ── -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ── Media access (Android 13+ granular media permissions) ── -->
    <!-- Android 13+ (API 33+): READ_MEDIA_IMAGES replaces READ_EXTERNAL_STORAGE for images -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Android 14+ (API 34+): VISUAL_USER_SELECTED for partial photo access -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- ── Legacy storage (Android 12 and below) ── -->
    <!-- maxSdkVersion=32 so it doesn't apply on Android 13+ where READ_MEDIA_* is used -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- ── Advertising ID (Android 13+ requirement — Play Console declaration) ── -->
    <!-- Required for apps targeting SDK 33+ that reference the advertising ID.
         Firebase + Google SDKs may reference it. Declared here for Play Console
         compliance. The app does NOT use ads for tracking — declare "No" in
         Play Console → Policy → App content → Advertising ID. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- ── Hardware features (all optional — app works without them) ── -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MalluCupid">
        <activity
            android:name="com.mallucupid.app.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MalluCupid"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging — push notifications -->
        <service
            android:name="com.mallucupid.app.notifications.MalluCupidMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
