<?xml version="1.0" encoding="utf-8"?>
<!-- Exclude sensitive data from auto-backup -->
<full-backup-content>
    <!-- Exclude the session store (contains access/refresh tokens) -->
    <exclude domain="sharedpref" path="mallu_cupid_session.xml"/>
</full-backup-content>
